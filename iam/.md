[accounts-api-policy.json]
This policy denies the pod's previous arbitrary AWS access and limits it to retrieving the accounts-api database secret and decrypting that secret through Secrets Manager; it grants no general IAM, compute, storage, or administrative permissions.
